// Etape 1 : Render when ready

/**
 * Info/Tips:
 * - isPlayerReady$ est un Observable qui va envoyer vrai quand l'image du joueur est chargée
 * - N'oubliez pas de subscribe();
 */

// import { filter, tap } from "rxjs";
// import { clearCanvas, drawThePlayer, isPlayerReady$ } from "../game.util";
